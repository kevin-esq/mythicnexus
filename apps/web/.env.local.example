# Copy this file to `.env.local` (same folder) and adjust values.
# `.env.local` is ignored by git.

# MythicNexus API (local or deployed). With Supabase Postgres, run the API using
# launch profile "Supabase" and keep this pointing at that process (default below).
NEXT_PUBLIC_API_URL=http://localhost:5118
